{
  "tasks": {
    "dev": "export $(cat .env | xargs) && deno run --allow-all --watch src/server.ts",
    "parse-day-one-podcasts-journal": "export $(cat .env | xargs) && deno run --allow-all src/parse-day-one-podcasts-journal.ts --",
    "parse-readwise-highlights": "export $(cat .env | xargs) && deno run --allow-all src/parse-readwise-highlights.ts"
  }
}